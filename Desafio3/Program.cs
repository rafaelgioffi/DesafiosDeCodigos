namespace Desafio3
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /* 3 - O diretor da competição, Prof. Poucher, tentou escolher uma sede que, de alguma
             * forma, ficasse o mais central possível para os vários participantes.
             * Para resolver isso, em um grande mapa ele marcou a posição dos participantes
             * prováveis da final. De posse desses dados o Prof. Poucher deseja escolher a sede 
             * mais central possível, computando o centro e o raio da menor circunferência que 
             * cobre todas as cidades marcadas no mapa (uma cidade está coberta se estiver no
             * interior ou borda desta circunferência).
             * 
             * Entrada - Esse problema é composto por várias instâncias. A primeira linha é 
             * composta por um inteiro n, 2 ≤ n ≤ 100, e indica o número de cidades. 
             * As próximas n linhas contém a descrição do posicionamento das cidades a partir
             * de suas coordenadas x e y no plano.
             * As coordenadas são números reais. Seu programa deve encerrar a execução quando 0
             * for o valor de n dado na entrada.
             * 
             * Saída - Para cada instância, imprima uma linha dizendo instancia k, onde k é o 
             * número da instância atual. Na segunda linha, imprima a coordenada x e a
             * coordenada y do centro e o raio da circunferência. Após cada instância, 
             * seu programa deve imprimir uma linha em branco.
             * 
             * Exemplo de entrada
             * 2
             * 0.0 0.0
             * 3 0
             * 5
             * 0 0
             * 0 1
             * 1 0
             * 1 1
             * 2 2
             * 0
             * 
             * Exemplo de saída
             * instancia 1
             * 1.50 0.00 1.50
             * Instancia 2
             * 1.00 1.00 1.41
             */ 
        }
    }
}